{
  "name": "vehicle-inspection-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Hack Day vehicle photo inspection demo with an optional Anthropic-powered analysis backend.",
  "license": "MIT",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js"
  }
}
